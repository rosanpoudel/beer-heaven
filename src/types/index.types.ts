export interface CardTypes {
  name: string;
  image_url: string;
  description: string;
  tagline: string;
  ingredients: any;
}

export interface DataItem {
  name: string;
  tagline: string;
  description: string;
}
